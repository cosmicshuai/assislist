# Todo System server — environment (gitignored)
# Database (dev-infra Postgres 16, peer auth over unix socket as cosmic)
DATABASE_URL=postgres://cosmic@/todo_system?host=/var/run/postgresql
# API Bearer token shared with Hermes capture skill
TODO_API_TOKEN=change-me-generate-with-openssl-rand-hex-32
# Bind
PORT=3456
HOST=192.168.1.180
