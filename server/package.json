{
  "name": "todo-system-server",
  "version": "0.1.0",
  "description": "Todo System API — capture + task tree + dependencies (Postgres)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "drizzle-orm": "1.0.0-beta.22",
    "express": "^5.2.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "drizzle-kit": "^1.0.0-beta.22"
  }
}
