{
  "name": "assislist-server",
  "version": "0.2.0",
  "description": "AssisList API — capture + task tree + dependencies (Postgres)",
  "type": "module",
  "main": "src/index.js",
  "license": "MIT",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "migrate": "node src/migrate.js",
    "test": "node --test 'test/**/*.test.js'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "drizzle-orm": "1.0.0-beta.22",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.1",
    "helmet": "^8.3.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "drizzle-kit": "^1.0.0-beta.22"
  }
}
